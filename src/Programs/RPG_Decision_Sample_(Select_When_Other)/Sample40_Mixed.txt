// Language: RPG (mixed free + fixed) - Mixed: pre-check then legacy WHENEQ ladder
// Mixed processing: Pre-check then legacy ladder
dcl-s flag ind;

if flag;
   callp preflight();
endif;

C                   SELECT                   Dept
C                   WHENEQ                   'HR'
C                   EXSR      DeptHR
C                   WHENEQ                   'FIN'
C                   EXSR      DeptFIN
C                   WHENEQ                   'IT'
C                   EXSR      DeptIT
C                   OTHER
C                   EXSR      DeptOther
C                   ENDSL

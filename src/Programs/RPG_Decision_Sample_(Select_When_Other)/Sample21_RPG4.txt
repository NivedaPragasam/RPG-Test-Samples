// Language: RPGLE (free format) - Blank/empty field check using %TRIM
// Treat blank field as "IS EMPTY"
dcl-s phone char(20);

select;
  when %trim(phone) = '';
     requestPhone();
  other;
     continue();
endsl;

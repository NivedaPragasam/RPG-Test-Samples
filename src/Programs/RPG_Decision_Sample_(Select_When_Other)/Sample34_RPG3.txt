// Language: RPGLE (free format) - Safe mode toggles with boolean flags
// Mode selection from flags
dcl-s safeMode ind;
dcl-s debugMode ind;

select;
  when safeMode and not debugMode;
     runSafe();
  when debugMode;
     runDebug();
  other;
     runNormal();
endsl;

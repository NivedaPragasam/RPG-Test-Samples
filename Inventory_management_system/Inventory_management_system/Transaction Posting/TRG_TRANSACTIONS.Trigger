-- Prevent direct inserts into TRANSACTIONS table
CREATE OR REPLACE TRIGGER INVENTORY.BI_TRANSACTIONS
NO CASCADE BEFORE INSERT ON INVENTORY.TRANSACTIONS
REFERENCING NEW AS N
FOR EACH ROW
BEGIN
  SIGNAL SQLSTATE '45000'
    SET MESSAGE_TEXT='Direct insert not allowed. Use SP_POST_TRANSACTION.';
END;
